const { Permission, PERMISSION_ACTIONS } = require('./Permission.model');
const Role = require('./Role.model');
const User = require('./User.model');

module.exports = {
  Permission,
  PERMISSION_ACTIONS,
  Role,
  User,
};

/** Mirrors backend naming — Admin / Member roles */
export const ROLE_NAME = Object.freeze({
  ADMIN: 'Admin',
  MEMBER: 'Member',
});

export const RESOURCES = Object.freeze({
  USERS: 'users',
  ROLES: 'roles',
  PERMISSIONS: 'permissions',
});

export const ACTIONS = Object.freeze({
  CREATE: 'create',
  READ: 'read',
  UPDATE: 'update',
  DELETE: 'delete',
});
